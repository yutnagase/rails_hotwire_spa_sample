<turbo-frame id="sale_form">
  <div class="bg-white shadow sm:rounded-lg">
    <div class="px-4 py-5 sm:p-6">
      <h3 class="text-lg leading-6 font-medium text-gray-900">売上編集</h3>
      
      <%= form_with model: @sale, local: false, class: "mt-5 space-y-6" do |form| %>
        <% if @sale.errors.any? %>
          <div class="bg-red-50 border border-red-200 rounded-md p-4">
            <div class="flex">
              <div class="ml-3">
                <h3 class="text-sm font-medium text-red-800">
                  <%= pluralize(@sale.errors.count, "error") %> prohibited this sale from being saved:
                </h3>
                <div class="mt-2 text-sm text-red-700">
                  <ul class="list-disc pl-5 space-y-1">
                    <% @sale.errors.full_messages.each do |message| %>
                      <li><%= message %></li>
                    <% end %>
                  </ul>
                </div>
              </div>
            </div>
          </div>
        <% end %>

        <div class="grid grid-cols-1 gap-6 sm:grid-cols-2">
          <div>
            <%= form.label :date, "日付", class: "block text-sm font-medium text-gray-700" %>
            <%= form.date_field :date, class: "mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm" %>
          </div>

          <div>
            <%= form.label :employee_id, "従業員", class: "block text-sm font-medium text-gray-700" %>
            <%= form.select :employee_id, options_from_collection_for_select(@employees, :id, :name, @sale.employee_id), 
                { prompt: "選択してください" }, 
                { class: "mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm" } %>
          </div>

          <div>
            <%= form.label :product_id, "商品", class: "block text-sm font-medium text-gray-700" %>
            <%= form.select :product_id, options_from_collection_for_select(@products, :id, :name, @sale.product_id), 
                { prompt: "選択してください" }, 
                { class: "mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm" } %>
          </div>

          <div>
            <%= form.label :customer_id, "取引先", class: "block text-sm font-medium text-gray-700" %>
            <%= form.select :customer_id, options_from_collection_for_select(@customers, :id, :name, @sale.customer_id), 
                { prompt: "選択してください" }, 
                { class: "mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm" } %>
          </div>

          <div>
            <%= form.label :quantity, "数量", class: "block text-sm font-medium text-gray-700" %>
            <%= form.number_field :quantity, class: "mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm" %>
          </div>

          <div>
            <%= form.label :amount, "金額", class: "block text-sm font-medium text-gray-700" %>
            <%= form.number_field :amount, class: "mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm" %>
          </div>
        </div>

        <div class="flex justify-end space-x-3">
          <%= link_to "キャンセル", sales_path, 
              class: "bg-white py-2 px-4 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500",
              data: { turbo_frame: "_top" } %>
          <%= form.submit "更新", class: "inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
        </div>
      <% end %>
    </div>
  </div>
</turbo-frame>